// Clases compartidas de UI (Tailwind v4 + tokens de globals.css).

/** Input/select/textarea de ancho completo con foco ámbar (tokens). */
export const inputClass =
  "w-full rounded-lg border border-border-strong bg-background text-foreground placeholder:text-subtle px-3 py-2 text-sm outline-none focus:border-accent focus:ring-2 focus:ring-ring/30";
